[pytest]
testpaths = tests
addopts = -v --html=report.html --self-contained-html
markers =
    smoke: Quick tests for core JSONAPI endpoints
    regression: Full regression suite for PUT and POST requests
